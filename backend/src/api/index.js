/**
 * 星禧水晶 - 自定义API路由
 * 
 * 在这个文件中注册所有自定义API端点
 */

module.exports = function (rootDirectory) {
  // 这里可以添加自定义路由
  // 例如：
  // const customRoutes = require("./routes/custom");
  // return customRoutes;
  
  return [];
};